# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# Django
*.log
db.sqlite3
staticfiles/
media/

# Environment
.env
.env.*

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Git
.git/
.gitignore

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Debug files
debug_files/

# Arquivos temporários
*.bak
*.tmp
.DS_Store

# Node modules (Tailwind)
theme/node_modules/
**/node_modules/

# Arquivos de backup
files/
**/files/

# Backups (raiz e aninhados — ex: production/backups, database/backups)
backups/
**/backups/
production/backups/
database/backups/

# Tarballs / dumps grandes que nunca devem entrar na imagem
# (dumps .sql de backup ficam sob **/backups/, já excluídos acima;
#  não excluímos *.sql global p/ preservar deploy/dev/init-db.sql)
*.tar
*.tar.gz
*.tgz
*.dump

# Media (uploads) aninhada — só top-level estava coberto
**/media/

# Docker dev backup
docker-compose.yml.bak

# Production-only files (não precisam ir na imagem dev)
# (Note: Dockerfile production COPY -> /app inclui tudo, então podem
# ficar disponíveis dentro do container, mas evitamos lixo grande)

