# URLs do app core
from django.urls import path

app_name = 'core'

urlpatterns = [
    # URLs específicas serão adicionadas conforme necessário
    # Por enquanto, o core é principalmente models + admin
]
