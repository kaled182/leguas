from django.apps import AppConfig


class PudoNetworkConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "pudo_network"
    verbose_name = "Rede PUDO (Lojas de Recolha)"
