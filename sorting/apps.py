from django.apps import AppConfig


class SortingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "sorting"
    verbose_name = "Sorting de Pacotes (Bigbags Virtuais)"
